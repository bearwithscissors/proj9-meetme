# These secrets apply to the whole MongoDB instance and must
# therefore be kept separate from the per-db client_secrets.py .
#
admin_user="wyattr"
admin_pw="MemeMachine"
port="27107"
host="localhost"
google_key_file = "secrets/google_key_file.json"
